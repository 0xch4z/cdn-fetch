platform :osx, '10.12'

target 'CDN Fetch' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # setup inherits

  target 'CDN FetchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CDN FetchUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  # Pods for CDN Fetch

  pod 'Alamofire', '~> 4.5'
  pod 'CKNavigation', '~> 0.1.2'
  pod 'STRegex', '~> 1.1'

end
